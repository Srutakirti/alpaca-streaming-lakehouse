[project]
name = "gcp-alpaca-datalake"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
    "numpy>=2.4.6",
    "pandas>=3.0.3",
]

[tool.uv.workspace]
members = ["extract", "load", "frontend"]
